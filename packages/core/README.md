# packages/core

Shared Relic domain constants, route identifiers, hierarchy types, AI task names, and MVP-safe guards.

This package must not call provider SDKs, LiteLLM, Supabase service-role APIs, or mutate canon.
