# Copy this file to .secrets.dev, then replace the placeholders locally.
# Never commit, paste into chat, or place either value in a browser variable.
OPENAI_API_KEY=
# LiteLLM requires its proxy master key to begin with sk-.
LITELLM_MASTER_KEY=
