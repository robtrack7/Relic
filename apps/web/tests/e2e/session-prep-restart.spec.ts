import { expect,test } from "@playwright/test";
test.skip(process.env.RELIC_E2E_AUTH!=="1","Requires completed D4 browser fixture.");
test("D4 future Sessions and Prep survive an application restart",async({page})=>{await page.goto("/auth/sign-in");await page.getByLabel("Email").fill("d4-browser@example.test");await page.getByLabel("Password").fill("password");await page.getByRole("button",{name:"Sign in"}).click();await expect(page).toHaveURL(/\/app(?:\/|$)/,{timeout:15_000});await page.getByRole("link",{name:"Sessions",exact:true}).click();await expect(page.getByText("Harbor Witness",{exact:true})).toBeVisible();await expect(page.getByText("Vault Descent",{exact:true})).toBeVisible();await page.getByRole("link",{name:"Prepare"}).last().click();await expect(page.getByLabel("Objective")).toHaveValue("Enter the vault");await expect(page.getByLabel("Scheduled date and time")).toHaveValue("2026-08-08T18:30");});
