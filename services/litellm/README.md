# services/litellm

LiteLLM proxy configuration and deployment notes will live here.

No provider keys or production secrets may be committed. App code references stable Relic model tiers only: `relic-fast`, `relic-balanced`, and `relic-deep`.
